Create HTML/CSS of an analog clock showing 10:10. Include numbers (or numerals) if you wish, and have a CSS animated second hand. Make it responsive and use a white background. Return ONLY the HTML/CSS code with no markdown formatting.

Save the result as a single self-contained file at `solution/clock.html` (HTML with inline CSS, no external dependencies).
